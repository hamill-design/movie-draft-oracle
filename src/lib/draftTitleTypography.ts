import type { CSSProperties } from 'react';

export const DRAFT_TITLE_FONT_STYLE: CSSProperties = {
  fontSize: 'clamp(48px, 12vw, 64px)',
  lineHeight: 'clamp(48px, 12vw, 64px)',
  maxWidth: '100%',
  overflowWrap: 'normal',
  wordBreak: 'normal',
};
